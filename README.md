# Bitasmbl-projectName5-91032a

## Description
Build a minimalist ASP.NET Core Web API for a "Developer Reading List" service, exposing clean CRUD endpoints for managing books and tags. The project focuses on HTTP API design, routing, status codes, and simple in-memory persistence to evaluate a junior back-end developer’s practical ASP.NET Core skills.

## Tech Stack
- ASP.NET Core

## Requirements
- Build a minimalist ASP.NET Core Web API
- CRUD endpoints for books and tags
- Clean HTTP API design, routing, and status codes
- Simple in-memory persistence
- Evaluate junior back-end ASP.NET Core skills

## Installation
- git clone https://github.com/MrBitasmblTester2/Bitasmbl-projectName5-91032a.git
- cd Bitasmbl-projectName5-91032a
- dotnet restore

## Usage
- dotnet run
- Access the HTTP API via the configured URL.

## Implementation Steps
1. Initialize ASP.NET Core Web API project.
2. Define in-memory models for books and tags.
3. Implement CRUD controllers for books and tags.
4. Configure routing and status codes.
5. Wire in-memory storage into controllers.
6. Test all endpoints.

## API Endpoints
- /books (CRUD)
- /tags (CRUD)